#include <iostream>

int main() {
  std::cout << "-----" << std::endl;
  std::cout << "Teacher Says: Rememeber the size of data types\n\t- char = 1 byte | integer & float  = 4 byte | double = 8 bytes |" << std::endl;

  std::cout << "Teacher Says: Its good coding pratices to Always delcare a intilize variable just in case" << std::endl;

  std::cout << "Note: Addresses are how many bites are up the array so if intArray[0] is 10 then intArray[1] is 14 bytes over" << std::endl;
  std::cout << "Note: Arrays are not objects in C++" << std::endl;
  std::cout << "Note: ++i in for loops are more efficent that i++!!!" << std::endl;
  std::cout << "Note: When passing array you enter the starting adress. all the blocks of memory are sequential and the single data type knows how many bits over is the next element of the array " << std::endl;
  return 0;
}